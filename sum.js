// export default (a, b) => {
//     return a + b;
// }
import {
    x,
    y
} from "./num.js";
const adder = (a, b) => a + b;
const sum = adder(x,y);
const name = "Charles";
export {
    sum,
    name
}